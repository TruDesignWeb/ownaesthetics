// All site assets are now converted and authored directly as .webp.
// This utility is retained as a safe no-op to eliminate speculative 404 network requests.
export default function preferWebp() {
  // No-op: all image references now directly point to verified webp assets.
}
